{
  "name": "docs",
  "version": "1.0.0",
  "description": "project ex",
  "scripts": {
    "init": "markbind init",
    "build": "markbind build docs",
    "serve": "markbind serve docs",
    "deploy": "markbind deploy"
  },
  "devDependencies": {
    "markbind-cli": "^5.1.0"
  }
}
